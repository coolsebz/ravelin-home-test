package main

import (
	"encoding/json"
	"log"
	"net/http"
	"strings"
	"testing"
)

// test sending a session req
func TestSessionRequests(t *testing.T) {

}

// test sending each of the event types as requests
func TestEvents(t *testing.T) {

}
